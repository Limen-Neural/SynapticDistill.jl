# Plan: Rename SpikenautDistill → SynapticDistill

**Issues**: [#10](https://github.com/Limen-Neural/SynapticDistill.jl/issues/10), [#4](https://github.com/Limen-Neural/SynapticDistill.jl/issues/4)
**Branch**: `rename/synapticdistill-package-identity`
**Effort**: Medium

---

## Goal

Rename the package identity from `SpikenautDistill` to `SynapticDistill` so it matches the repo name. 34 occurrences across 10 files.

---

## Changes

### 1. File rename
- `src/SpikenautDistill.jl` → `src/SynapticDistill.jl`

### 2. Module definition (src/SynapticDistill.jl, the renamed file)
- Line 4: docstring `SpikenautDistill.jl` → `SynapticDistill.jl`
- Line 9: `module SpikenautDistill` → `module SynapticDistill`

### 3. Project.toml
- Line 1: `name = "SpikenautDistill"` → `name = "SynapticDistill"`

### 4. Manifest.toml
- Line 707: `[[deps.SpikenautDistill]]` → `[[deps.SynapticDistill]]`

### 5. Source docstrings
- `src/types.jl` L4: `Core data structures for SpikenautDistill.` → `SynapticDistill.`
- `src/training.jl` L4: `Main training entry point for SpikenautDistill.` → `SynapticDistill.`

### 6. Test file (test/runtests.jl) — 13 occurrences
- L2: `using SpikenautDistill` → `using SynapticDistill`
- L5: `@testset "SpikenautDistill"` → `"SynapticDistill"`
- L8-9: `SpikenautDistill` → `SynapticDistill`
- L10-13: `isdefined(SpikenautDistill, ...)` → `isdefined(SynapticDistill, ...)`
- L36, L60, L68: `SpikenautDistill.W_MIN`/`.W_MAX` → `SynapticDistill.W_MIN`/`.W_MAX`

### 7. Examples
- `examples/pure_snn_training.jl` L1: `using SpikenautDistill` → `using SynapticDistill`
- `examples/hybrid_olmoe_training.jl` L1: same

### 8. Benchmarks
- `benchmarks/eprop_tick_benchmark.jl` L1: `using SpikenautDistill` → `using SynapticDistill`
- `benchmarks/eprop_tick_benchmark.jl` L41: benchmark title string
- `benchmarks/latest_results.txt` L1: title string

### 9. README.md — 10 occurrences
- L1: `# SpikenautDistill.jl` → `# SynapticDistill.jl`
- L8, L47, L78: backtick prose references
- L21, L65: `using SpikenautDistill` in code blocks
- L43, L65: file URL paths `SpikenautDistill.jl/examples/...`
- L80-81: ecosystem prose references
- L87: license-change note about the rename (update to say "completed" or remove reference)

---

## Execution order

1. `git checkout -b rename/synapticdistill-package-identity`
2. `git mv src/SpikenautDistill.jl src/SynapticDistill.jl`
3. Edit `src/SynapticDistill.jl` (module name + docstring)
4. Edit `Project.toml` (package name)
5. Edit `Manifest.toml` (deps block)
6. Edit `src/types.jl`, `src/training.jl` (docstrings)
7. Edit `test/runtests.jl` (all 13 references)
8. Edit `examples/*.jl` (2 files)
9. Edit `benchmarks/eprop_tick_benchmark.jl` (2 refs)
10. Edit `benchmarks/latest_results.txt` (1 ref)
11. Edit `README.md` (all references)

---

## Verification

```bash
# 1. Confirm zero stale references remain
grep -rn "SpikenautDistill" Project.toml Manifest.toml src/ test/ examples/ benchmarks/ README.md

# 2. Package loads
julia --project=. -e 'using SynapticDistill; println("OK")'

# 3. Tests pass
julia --project=. -e 'using Pkg; Pkg.test()'

# 4. Check module exports
julia --project=. -e 'using SynapticDistill; println(names(SynapticDistill))'
```
